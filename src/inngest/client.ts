/**
 * Inngest client initialization.
 *
 * Status: PENDING — install `inngest` (I-7).
 * TODO(I-7): `export const inngest = new Inngest({ id: "catalogvector" });`
 */
export const inngest = null; // TODO(I-7)

export type InngestClient = typeof inngest;
