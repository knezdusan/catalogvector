/**
 * Drizzle ORM setup.
 *
 * Status: PENDING — install `drizzle-orm`, `pg`, `drizzle-kit` (I-5).
 * TODO(I-5): replace with a real `drizzle()` client bound to env.DATABASE_URL.
 */
export const db = null; // TODO(I-5)

export type Db = typeof db;
