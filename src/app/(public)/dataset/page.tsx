/**
 * Dataset table — PLACEHOLDER.
 *
 * Future home of PUB-1 (aggregate + anonymised worked examples). Named per-store
 * results are NEVER published here — released privately on request, which is the
 * lead-capture mechanism (BLUEPRINT.md PUB-1, TDD.md C9).
 *
 * Status: PENDING (BLUEPRINT.md PUB-1).
 */
export default function DatasetPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 py-32">
      <h1 className="text-3xl font-semibold tracking-tight">Dataset</h1>
      <p className="mt-4 text-zinc-600 dark:text-zinc-400">
        Pending publication (PUB-1).
      </p>
    </main>
  );
}
