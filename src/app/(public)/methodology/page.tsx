/**
 * Methodology write-up — PLACEHOLDER.
 *
 * Future home of PUB-2 (methodology). Must state limitations explicitly,
 * including the metafield blind spot (TDD.md §2.3, §6.3).
 *
 * Status: PENDING (BLUEPRINT.md PUB-2).
 */
export default function MethodologyPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 py-32">
      <h1 className="text-3xl font-semibold tracking-tight">Methodology</h1>
      <p className="mt-4 text-zinc-600 dark:text-zinc-400">
        Pending publication (PUB-2).
      </p>
    </main>
  );
}
