/**
 * Probe: U-5 — do Catalog results reflect merchant metafields at all? (TDD.md §2.7)
 *
 * Throwaway script. NOT part of the app. Run with: npx tsx scripts/u5-metafield-probe.ts
 *
 * **U-5 can invalidate the methodology.** Scheduled Week 1, first.
 * Seed a dev store with structured metafields, wait out the §2.6 catalog freshness
 * delay, compare Catalog results with and without the metafields.
 *
 * Status: PENDING — implement Week 1, first (TDD.md §9, §10 Q-5).
 */
// TODO(Week 1): implement. Blocked on I-2 (dev store) + I-4 (agent profile).
export {};
