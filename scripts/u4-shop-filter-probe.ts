/**
 * Probe: U-4 — is `filters.shops` a hard restriction or a soft bias? (TDD.md §2.7)
 *
 * Throwaway script. NOT part of the app. Run with: npx tsx scripts/u4-shop-filter-probe.ts
 *
 * **U-4 can invalidate the methodology.** Scheduled Week 1, first.
 * Control experiment: query for a product known present in a scoped shop; observe
 * whether unscoped results leak in or scoped results are dropped.
 *
 * Status: PENDING — implement Week 1, first (TDD.md §9, §10 Q-4).
 */
// TODO(Week 1): implement. Blocked on I-4 (UCP agent profile).
export {};
